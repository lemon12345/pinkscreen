import PinkScreenClient from "@/components/home/PinkScreenClient";

export default async function HomeComponent() {
  return (
    <main className="w-full">
      {/* SEO-friendly structure with single h1 */}
      <PinkScreenClient />
    </main>
  );
}
